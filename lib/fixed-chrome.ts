/** Fixed top/bottom site nav — always above featured card reveal (`z-index: 50`). */
export const FIXED_SITE_NAV_Z_CLASS = "z-[60]";

/** Global horizontal gutter (`main`, footer, fixed chrome). */
export const SITE_PAGE_GUTTER = "px-4 sm:px-10 lg:px-[60px]";

export const SITE_CONTENT_MAX_WIDTH_CLASS = "max-w-[1320px]";

/** Reserved width matching `CaseNavPrevNextButton` (40×40) on mobile bottom bar. */
export const MOBILE_NAV_EDGE_SLOT_CLASS = "h-10 w-10 shrink-0";
